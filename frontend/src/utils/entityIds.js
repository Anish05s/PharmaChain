/** Entity ID prefixes: {prefix}-{serial} e.g. mfg-001, sup-002, con-100000 */
export const ENTITY_PREFIX = {
  manufacturer: 'mfg',
  supplier: 'sup',
  consumer: 'con',
}

/** Demo / seed defaults (first entity per type) */
export const DEFAULT_ENTITY_ID = {
  manufacturer: 'mfg-001',
  supplier: 'sup-001',
  consumer: 'con-001',
}
